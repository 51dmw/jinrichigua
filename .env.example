# ─────────────────────────────────────────────────────────────
# 根级 .env.example — 复制为 .env 并按需填写。
# MUST NOT 把真实密钥提交入库（§1 / §9）。
# 各应用另有自己的 .env.example（apps/cms、apps/web）。
# ─────────────────────────────────────────────────────────────

# ===== 公共 =====
NODE_ENV=development

# 前台公开访问的站点地址（用于 canonical / sitemap / OG）
SITE_URL=http://localhost:3000

# Strapi 对外 API 地址（前台读数用）
STRAPI_API_URL=http://localhost:1337
